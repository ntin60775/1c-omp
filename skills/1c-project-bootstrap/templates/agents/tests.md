# Каталог tests — тесты

Всё, что нужно только для прогона тестов и не входит в поставку.

## Правила
- `cfe/` — исходники тестовых расширений (YAxUnit), `epf/` — тестовых обработок.
- Скриптовые тесты OneScript (`*.os`) — прямо здесь.

