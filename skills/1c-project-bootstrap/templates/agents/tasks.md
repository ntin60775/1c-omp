# Каталог tasks — задачи и скрипты OScript

Скрипты `.os` и определения задач для сборки и автоматизации (vrunner, opm).

## Правила
- Один скрипт — одна задача; имена осмысленные.
- Параметры подключения к ИБ не хардкодить — брать из `env.json`/профиля запуска.

